export interface IIngredientsList {
  name: string;
  price: number;
  isActive: boolean;
  img: any;
}

export interface ISizeList {
  size: string;
  price: number;
  isActive: boolean;
}
